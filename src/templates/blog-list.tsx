import React, { FC } from 'react';
import type { PageProps, HeadFC } from 'gatsby';
import SEO from '../components/seo.component';

const BlogList: FC<PageProps> = props => {
  return <></>;
};

export const Head: HeadFC = ({ data }) => <SEO />;

export default BlogList;
